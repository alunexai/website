---
import Lines from '@assets/Lines.astro';
import Button from '@components/Button.astro';
import {Image} from 'astro:assets';
import welcomeBackground from '@assets/img/welcome-background.png';
import welcomeChat from '@assets/img/welcome-chat.png';

const title = 'Increase sales & productivity through artificial intelligence';
const subTitle = 'Improved margins for SMBs are just a chat away';
const btnText = 'Get started';
---

<div
  class="pt-[70px] flex flex-col items-center relative w-full h-[1420px] overflow-hidden"
>
  <div class="absolute inset-0 top-[-70px]">
    <Image
      src={welcomeBackground}
      alt="Background"
      class="w-full h-full object-cover"
    />
  </div>

  <div class="relative z-10">
    <Lines />
  </div>

  <h1
    class="gradient-text mt-[32px] w-[1000px] text-h1 leading-[64px] text-center font-bold relative z-10"
  >
    {title}
  </h1>

  <h4
    class="mt-[32px] text-color-main text-h4 leading-[20px] text-center font-medium relative z-10"
  >
    {subTitle}
  </h4>

  <Button
    text={btnText}
    variant="main"
    size="lg"
    className="mt-[32px] text-h6 font-medium relative z-10"
  />

  <div class="absolute bottom-20">
    <Image
      src={welcomeChat}
      alt="Chat"
      width="707px"
      height="660px"
      class="object-cover"
      quality="100"
      format="png"
    />
  </div>
</div>

<style>
  .gradient-text {
    background: linear-gradient(100deg, #dfffe6, #7db9ff);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }
</style>
