# alunex-website conventions

## Git workflow

- Direct commits to `main` are fine for minor copy/content edits.
- Use a feature branch + PR for integrations (e.g. lead-capture forms) or functional components (e.g. user auth) — these are where CI tests will start to accumulate, and a review checkpoint is worth the friction.
